from pathlib import Path

WORKFLOW = Path('.github/workflows/update-offset-goal-docs.yml')
SELF = Path('tools/update_offset_goal_docs.py')


def replace_once(text: str, old: str, new: str, label: str) -> str:
    count = text.count(old)
    if count != 1:
        raise RuntimeError(f'{label}: expected one match, got {count}')
    return text.replace(old, new, 1)


spec_path = Path('docs/SPEC.md')
spec = spec_path.read_text(encoding='utf-8')
spec = replace_once(
    spec,
    """## 9. ステージ

10種類の名称を持つが、現時点では`makeStage(name, shortName, variant)`による共通生成型。選択は`STAGES[Math.floor(Math.random() * STAGES.length)]`であり、同じステージが連続する可能性がある。

実機評価では、中央を縦に掘るだけで成功でき、ステージ差・ルート判断・戦略性が不足している。現状のステージは完成仕様ではない。

次のP0-Cで実施すること:

- 3クラスタを独立した初期室へ分ける
- 1室だけの解放では20個へ届かない構造にする
- 中央一直線ルートを固定壁とオフセット開口で遮断する
- 上層・中上層・中下層・下層に意思決定を置く
- 安全路、短距離危険路、高得点路を分ける
- ステージ1を先に完成させてから残り9ステージを個別化する
""",
    """## 9. ステージ

10ステージからランダム選択する。同じステージが連続する可能性は残る。

ステージ1は3室選択型として個別定義済み。残り9ステージは共通生成型だが、全10ステージで中央ゴールと中央一本掘りを禁止する。

### 9-1. 左右ゴール

- 左ゴール: `x=28`, 幅116, 中心X=86
- 右ゴール: `x=176`, 幅116, 中心X=234
- ゴール中心Xが120〜200の中央帯になることを禁止
- 狐絵文字は回収枠中央へ描画

### 9-2. S字障壁

- 左ゴール: 中段は右開口、最終層は左開口
- 右ゴール: 中段は左開口、最終層は右開口
- 中段と最終開口を同じ側にしない
- 開口幅は8セル
- 中央を上から下へ一本掘るだけでは通過できない

### 9-3. ステージ1

3室から最低2室を開放し、中段の左開口へ寄せ、下層で右へ戻して右端の狐へ届ける。

### 9-4. 残る未完成項目

- ステージ2〜10の完全な個別データ化
- 10ステージ一巡方式
- 各ステージ固有の別解・危険・仕掛け調整
- 成功率とスコア中央値による公平性測定

詳細は`docs/OFFSET_GOAL_ZIGZAG_SPEC.md`を参照する。
""",
    'SPEC stage section',
)
status_start = spec.index('## 15. 実装・レビュー状態')
spec = spec[:status_start] + """## 15. 実装・レビュー状態

- PR #24: 固定間隔物理と平面振動候補修正をmainへ反映済み。実機で振動解消は継続確認
- PR #26: 精密掘削をmainへ反映済み
- PR #27: ステージ1の3室選択構造をPR #26経由でmainへ反映済み
- 左右ゴール・S字導線: `feat/tabakon-offset-goals-zigzag`でレビュー中

現在の最優先実機確認:

1. 全ステージで中央一本掘りだけでは成功しない
2. 狐が左端または右端に明確に見える
3. 中段で狐と反対側へ寄せ、最終層で狐側へ戻す必要がある
4. 60秒以内に20個届ける現実的なルートが残る
5. 平面振動と掘削幅に回帰がない

残るP0:

- 左右ゴール・S字導線の実機クリア可能性
- スクロールバーが初見で上下操作と認識できるUI
- ステージ2〜10の完全な個別化と戦略差
"""
spec_path.write_text(spec, encoding='utf-8')

plan_path = Path('docs/IMPLEMENTATION_PLAN.md')
plan = plan_path.read_text(encoding='utf-8')
plan = replace_once(
    plan,
    """- PR #25: 実機P0ブロッカーと物理修正条件の文書同期 — mainへマージ済み
- P0-B精密掘削: `feat/tabakon-p0b-precision-carving`で実装中。実機合格前のDraft対象
""",
    """- PR #25: 実機P0ブロッカーと物理修正条件の文書同期 — mainへマージ済み
- PR #26: 半径11px・補間5px・1ストローク1回砂崩れ — mainへマージ済み
- PR #27: ステージ1の3室選択構造 — PR #26経由でmainへ反映済み
- 左右ゴール・S字導線: `feat/tabakon-offset-goals-zigzag`でレビュー中
""",
    'plan current state',
)
old_p0c = """### P0-C コアゲーム性

現状は中央を縦に掘るだけでクリアできる。次のPRでステージ1だけを先行再設計する。

1. 3クラスタを独立した初期室へ分割
2. 1室の10個だけでは成功できない構造
3. 中央一直線ルートを固定壁と左右へずれた開口で遮断
4. 上層・中上層・中下層・下層へ意思決定を配置
5. 安全路、短距離危険路、高得点路を分離
6. 色別🚬の初期配置をルート選択へ結び付ける
7. ステージ1だけを実機合格させる
8. 合格後に残り9ステージを個別定義
"""
new_p0c = """### P0-C コアゲーム性

ステージ1の3室選択構造は実装済みだが、実機評価で中央付近の狐へ真下に落とす単調さが残った。

追加修正として全10ステージへ左右ゴールとS字障壁を導入する。

1. 狐を左端または右端へ配置
2. 中段開口を狐と反対側へ配置
3. 最終開口を狐と同じ側へ配置
4. 中段と最終開口を同じ側にしない
5. 中央一本掘りを固定壁で停止
6. ステージ1は左へ寄せてから右の狐へ戻す
7. ステージ2〜10も左右交互のS字経路にする
8. 自動検査で中央ゴール・同側開口・壁欠損を拒否

この修正後も、残り9ステージの完全な個別化は別作業として残る。
"""
plan = replace_once(plan, old_p0c, new_p0c, 'plan P0-C')
old_order = """1. 平面振動の実機再確認。不合格なら物理へ戻る
2. **P0-B精密掘削の実機確認と値調整**
3. **P0-Cステージ1のコアゲーム性再設計**
4. P0-DスクロールUIと初回説明
5. 残り9ステージの個別化と10ステージ一巡方式
6. ステージ別成功率・スコア中央値による公平性調整
7. デバッグ、自動検査、長時間試験
8. Supabase実疎通、Codeberg、実験場を含む公開準備

P0-Bのコード実装完了だけでP0-Cへ進めない。実機で掘削幅・連続性・指離し時負荷を確認する。

## 第4部：次回判断

現在の次の判断:

> `feat/tabakon-p0b-precision-carving`の半径11px・補間5px・ストローク単位砂崩れが、iPhone実機で「狙った細道を作れるが細すぎない」操作感になっているか。

不合格時は、ステージへ進まずP0-Bの定数または砂崩れ範囲だけを再調整する。
"""
new_order = """1. **左右ゴール・S字導線の実機クリア可能性確認**
2. 平面振動と精密掘削の回帰確認
3. P0-DスクロールUIと初回説明
4. ステージ2〜10の完全な個別化
5. 10ステージ一巡方式
6. ステージ別成功率・スコア中央値による公平性調整
7. デバッグ、自動検査、長時間試験
8. Supabase実疎通、Codeberg、実験場を含む公開準備

## 第4部：次回判断

現在の次の判断:

> 全10ステージで、中央を縦に一本掘るだけでは成功せず、中段で狐と反対側へ寄せ、最終層で狐側へ戻せば60秒以内に20個届けられるか。

不合格時は、成功条件や制限時間を変更せず、開口幅・事前空洞・危険帯の位置だけを再調整する。
"""
plan = replace_once(plan, old_order, new_order, 'plan order')
plan_path.write_text(plan, encoding='utf-8')

check_path = Path('docs/REVIEW_CHECKLIST.md')
check = check_path.read_text(encoding='utf-8')
old_check = """## J. P0-B後も未実装

### J-1. コアゲーム性

- [ ] 中央を縦に掘るだけでは20個届かない
- [ ] 3クラスタを独立した初期室へ分ける
- [ ] 1室だけの解放では成功できない
- [ ] 上層・中上層・中下層・下層で最低2回以上のルート判断が必要
- [ ] 安全路、短距離危険路、高得点路を選べる
- [ ] 色別🚬の配置がルート判断と結び付いている
- [ ] ステージ1を先行完成し、実機合格前に残り9ステージを量産しない

### J-2. スクロールUI
"""
new_check = """## J. 左右ゴール・S字導線

### J-1. 静的契約

- [ ] 全10ステージに`routePattern`がある
- [ ] 左ゴール中心Xは86、右ゴール中心Xは234
- [ ] ゴール中心Xが120〜200の中央帯にない
- [ ] 中段開口と最終開口が反対側
- [ ] 最終開口と狐が同じ側
- [ ] 開口幅は8セル
- [ ] 指定開口以外は固定壁で連続している
- [ ] 狐絵文字が回収枠中央に描画される
- [ ] `validateOffsetGoal()`の警告が0

### J-2. 実機合格

- [ ] 画面中央を上から下へ一本なぞるだけでは成功しない
- [ ] 中段の固定壁で中央落下が止まる
- [ ] 狐が左端または右端に明確に見える
- [ ] 中段で狐と反対側へ横誘導する必要がある
- [ ] 最終層で狐側へ戻す必要がある
- [ ] 上画面と下画面を最低1回ずつ確認する
- [ ] 60秒以内に20個届く現実的なルートがある
- [ ] 必須開口が危険ゾーンで完全に塞がれていない
- [ ] 平面振動と掘削幅に回帰がない

### J-3. スクロールUI
"""
check = replace_once(check, old_check, new_check, 'review offset goals')
check = check.replace('### J-3. ステージ差', '### J-4. ステージ差', 1)
check_path.write_text(check, encoding='utf-8')

for path in (WORKFLOW, SELF):
    if path.exists():
        path.unlink()

print('offset goal docs synchronized')
